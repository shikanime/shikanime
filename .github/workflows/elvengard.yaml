name: Build Elven Gard
on:
  push:
    tags:
      - v*.*.*
jobs:
  build:
    runs-on: ubuntu-22.04
    environment: shikanime-studio
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v2
      - name: Prepare build environment
        uses: cachix/install-nix-action@v15
        with:
          extra_nix_config: system-features = nixos-test benchmark big-parallel kvm
      - name: Connect Shikanime Nix cache
        uses: cachix/cachix-action@v10
        with:
          name: shikanime
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
      - name: Build image
        run: nix build .#nixosConfigurations.elvengard.config.system.build.hypervImage
      - name: Export output path
        run: echo "RELEASE_PATH=$(readlink -f result)" >> "$GITHUB_ENV"
      - name: Export build artifacts
        uses: actions/upload-artifact@v2
        with:
          name: Elven Gard
          path: ${{ env.RELEASE_PATH }}
