name: Update Wakatime Stats
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
jobs:
  update:
    runs-on: ubuntu-22.04
    environment: shikanime-studio
    steps:
      - name: Update Github Gist
        uses: matchai/waka-box@v4.0.5
        env:
          GH_TOKEN: ${{ secrets.WAKABOX_GITHUB_TOKEN }}
          GIST_ID: ${{ secrets.WAKABOX_GITHUB_GIST_ID }}
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
